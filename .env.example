# DB (T-004)。07_人間作業チェックリスト.md A-3参照。
# DATABASE_URL: Neonのpooled接続(アプリ実行時)。ローカルはdocker-compose.test.yml参照。
DATABASE_URL="postgresql://ddia:ddia@localhost:5433/ddia_test?schema=public"
# DIRECT_URL: Neonのdirect接続(migrate/introspection用)。ローカルはDATABASE_URLと同じでよい。
DIRECT_URL="postgresql://ddia:ddia@localhost:5433/ddia_test?schema=public"
