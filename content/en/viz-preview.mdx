# Viz verification page: IsolationViz

This is a fixed page for verifying T-208 (IsolationViz) through the real
`<Viz>` MDX lazy-load path, without waiting for Ch7 curriculum content
(T-210 onward). Like `/lab-preview`, it is not part of the production
curriculum.

<Viz name="isolation" preset="dirty-read" />
