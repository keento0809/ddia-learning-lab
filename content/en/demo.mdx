# Dummy Lesson: Sum an Array

This is dummy content for the Walking Skeleton (T-000). It is not real curriculum
material — it exists only to verify that the pipeline of
"load MDX → render page → run and grade an exercise" is actually connected end to end.

The exercise below asks you to implement a `sum` function that adds up all elements
of an array. Confirm both that the "correct solution" button passes, and that the
"infinite loop" button times out.
