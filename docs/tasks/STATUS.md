# 実装進捗 (WBS: docs/design/03\_実装タスク分割書.md)

最終更新: 2026-07-19

## Phase 0: 基盤

| タスク | 名称                         | サイズ | 状態 | ブランチ/PR | 備考 |
| ------ | ---------------------------- | ------ | ---- | ----------- | ---- |
| T-000  | Walking Skeleton             | L      | 🔄   | feat/T-000-walking-skeleton | docs/skeleton-notes.md参照 |
| T-001  | リポジトリ基盤整備           | M      | 🔄   | feat/T-001-repo-foundation | Tailwind/ESLint(no-literal-jsx-text)/Prettier/Vitest追加。lint/typecheck/test/build全green |
| T-002  | CIパイプライン               | S      | 🔄   | feat/T-002-ci | lint→typecheck→unit→build(直列)+ADR-007バンドルサイズゲート(2.5MiB警告/3MiB失敗)。content-validate/e2eはplaceholder(if: false) |
| T-003  | i18n骨格                     | M      | 🔄   | feat/T-003-i18n | next-intlによる`app/[locale]`ルーティング+middleware。lint/typecheck/test/build全green |
| T-010  | コントラクト定義             | M      | 🔄   | feat/T-010-contracts |      |
| T-004  | DB/Prisma                    | M      | 🔄   | feat/T-004-db-prisma | prisma/schema.prisma(02§2.1全8テーブル)+初回migration+seed(2名+進捗3件)+lib/db.ts(driver adapter)+docker-compose.test.yml+CRUD統合テスト8件。lint/typecheck/test/build全green |
| T-005  | 認証                         | M      | 🔄   | feat/T-005-auth | Auth.js v5(Credentials: Argon2id純WASM実装+scrypt(node:crypto)フォールバック、GitHub/Googleは環境変数未設定時UI非表示)+カスタムPrismaアダプタ(JWTセッション、02§2.1のusers/oauth_accountsのみ使用)。セッションCookie(authjs.session-token)はHTTPOnly/SameSite=Lax。`/api/auth/signup`・レート制限ミドルウェア(5req/min/IP、/api/auth/*のみ)・`/{ja,en}/auth/{signin,signup,reset,reset/confirm}`画面。パスワードリセットは新規DBテーブルなしのステートレスJWT(passwordHashダイジェスト照合で使い切り制約を実現)+メール送信基盤未導入のためリンクを画面上に直接表示。統合テスト(signup→credentials sign-in→session、実Auth.jsハンドラ+実Cookieヘッダで検証)・単体テスト(パスワードハッシュ/フォールバック/レート制限/OAuth表示分岐/リセットトークン)追加。lint/typecheck/test/build/test:integration全green |
| T-006  | コンテンツビルドパイプライン | L      | 🔄   | feat/T-006-content-pipeline | lib/content.ts(ビルド時MDX/YAMLロード+frontmatter検証)、scripts/validate-content.ts(①slug集合一致②testsハッシュ一致③リンク切れ④frontmatter必須項目)、slugマニフェストJSON生成、CI content-validateジョブ有効化 |
| T-007  | 共通レイアウト               | M      | 🔄   | feat/T-007-layout | Header(ロゴ/ナビ/言語トグル/テーマ切替/アカウントメニュー、Radix DropdownMenu)+Footer+ロケール内404+Error Boundary(error.tsx/global-error.tsx)。テーマ状態はzustand+persist、beforeInteractiveスクリプトでFOUC防止。Playwright+axe-coreでキーボード操作+a11y(critical 0件)を検証。qa-evaluator: 初回FAIL(ダーク+リロード時hydration警告、未実装リンクの本番prefetchで自動404×4)→suppressHydrationWarning・prefetch={false}で修正しPASS(操作性5/i18n品質5/誠実性5)。lint/typecheck/test/build/e2e全green |

## Phase 1: MVP

| タスク  | 名称                        | サイズ | 状態 | ブランチ/PR | 備考 |
| ------- | --------------------------- | ------ | ---- | ----------- | ---- |
| T-101   | カリキュラム一覧 (S-02)     | M      | 🔄   | feat/T-101-curriculum | `app/[locale]/learn/page.tsx`(SSG)。module.yamlスキーマ(lib/contracts/module.ts)に基づき3部(Part I〜III、orderの範囲から導出)へセクション分割し`ModuleCard`(タイトル/所要時間/レッスン数/進捗リング)で描画。ロック無し+`OrderConnector`で推奨順を矢印表示。進捗リングはprops注入のみ(実データ接続はT-105)。`lib/content.ts`(fs依存)をpage.tsxから直接importせずビルド時生成JSON(`scripts/generate-curriculum.ts`→`lib/generated/curriculum.{ja,en}.json`、`lib/curriculum.ts`が通常importで取り込む)経由にする設計とし、`npm run{build,typecheck,test,dev}`のpre-hookで自動生成。フィクスチャ12モジュール(`tests/fixtures/curriculum/`)を用いた順序描画のスナップショットテスト2件+`groupModulesByPart`の単体テスト6件。lint/typecheck/test/build全green、`next start`実アクセス(console error 0、言語トグルで`/ja/learn`⇄`/en/learn`保持を確認)、`wrangler dev`実アクセス(200、fsエラー無し)も確認済み |
| T-102   | モジュール詳細 (S-03)       | S      | 🔄   | feat/T-102-module-detail | `app/[locale]/learn/[module]/page.tsx`。レッスン/クイズ/演習の目次(`lib/moduleDetail.ts`のbuildModuleToc、lesson→quiz→exerciseの順、lessonはfrontmatter order昇順)、所要時間合計(module.yamlのminutes)、次アイテム導線(目次先頭へのCTA、進捗連動はT-105)を`components/module/ModuleDetail.tsx`で描画。存在しないslugはnotFound()で404。lint/typecheck/test/build全green、test-integrity-reviewer実施し問題なしを確認 |
| T-103   | レッスンページ (S-04)       | L      | ⏳   | —           |      |
| T-104   | 進捗API                     | M      | 🔄   | feat/T-104-progress-api | `PUT/GET /api/progress`(slugマニフェスト照合→409/statusの単調性/ストリーク/CSRFダブルサブミットcookie/RFC 9457)。API統合テスト9件(正常/400/401/403/409/単調性/GETモジュールフィルタ/ストリーク日跨ぎ)+単体テスト14件(streak/csrf)。lint/typecheck/test/test:integration/build全green。test-integrity-reviewer実施し問題なしを確認 |
| T-105   | 進捗クライアント統合        | M      | ⏳   | —           |      |
| T-106   | クイズ (S-05)               | M      | ⏳   | —           |      |
| T-107a  | Workerハーネス              | M      | 🔄   | worktree-T-107a-worker-harness | 静的チェック・危険グローバル無効化・Blob URL import・console捕捉(200件)・結果1MB上限+truncatedを実装。lint/typecheck/test/build全green |
| T-107b  | 採点器 (grader)             | M      | 🔄   | worktree-T-107b-grader | `lib/runner/grader.ts`。4種assert(equals/deepEquals/oneOf/matches)+property(演習別ヘルパ登録機構)+complexity(参考表示)、部分点(score=round(passed/total*100))、失敗時diff生成、graderVersion。エッジケーステーブル駆動テスト(NaN/-0/循環参照拒否/深いネスト)全合格。lint/typecheck/test/build全green |
| T-107c  | Runner統合                  | M      | ⏳   | —           |      |
| T-108   | 演習ページ (S-06)           | L      | ⏳   | —           |      |
| T-109   | 提出API                     | S      | ⏳   | —           |      |
| T-110-1 | Part I 教材 JA: モジュール1 | M      | ⏳   | —           |      |
| T-110-2 | Part I 教材 JA: モジュール2 | M      | ⏳   | —           |      |
| T-110-3 | Part I 教材 JA: モジュール3 | M      | ⏳   | —           |      |
| T-110-4 | Part I 教材 JA: モジュール4 | M      | ⏳   | —           |      |
| T-111-1 | Part I 教材 EN: モジュール1 | M      | ⏳   | —           |      |
| T-111-2 | Part I 教材 EN: モジュール2 | M      | ⏳   | —           |      |
| T-111-3 | Part I 教材 EN: モジュール3 | M      | ⏳   | —           |      |
| T-111-4 | Part I 教材 EN: モジュール4 | M      | ⏳   | —           |      |
| T-112   | ダッシュボード (S-07)       | M      | ⏳   | —           |      |
| T-113   | ゲスト進捗                  | S      | ⏳   | —           |      |

## Phase 2: 分散データ編

| タスク  | 名称                         | サイズ | 状態 | ブランチ/PR | 備考 |
| ------- | ---------------------------- | ------ | ---- | ----------- | ---- |
| T-201   | SQLランナー                  | M      | ⏳   | —           |      |
| T-202   | SQL演習UI                    | S      | ⏳   | —           |      |
| T-203   | Viz共通基盤                  | M      | ⏳   | —           |      |
| T-204   | LsmTreeViz                   | L      | ⏳   | —           |      |
| T-205   | HashRingViz                  | L      | ⏳   | —           |      |
| T-206   | ReplicationLagViz            | L      | ⏳   | —           |      |
| T-207   | RaftViz                      | L      | ⏳   | —           |      |
| T-208   | IsolationViz                 | L      | ⏳   | —           |      |
| T-210-1 | Part II 教材 JA: モジュール5 | M      | ⏳   | —           |      |
| T-210-2 | Part II 教材 JA: モジュール6 | M      | ⏳   | —           |      |
| T-210-3 | Part II 教材 JA: モジュール7 | M      | ⏳   | —           |      |
| T-210-4 | Part II 教材 JA: モジュール8 | M      | ⏳   | —           |      |
| T-210-5 | Part II 教材 JA: モジュール9 | M      | ⏳   | —           |      |
| T-211-1 | Part II 教材 EN: モジュール5 | M      | ⏳   | —           |      |
| T-211-2 | Part II 教材 EN: モジュール6 | M      | ⏳   | —           |      |
| T-211-3 | Part II 教材 EN: モジュール7 | M      | ⏳   | —           |      |
| T-211-4 | Part II 教材 EN: モジュール8 | M      | ⏳   | —           |      |
| T-211-5 | Part II 教材 EN: モジュール9 | M      | ⏳   | —           |      |

## Phase 3: 派生データ編・付加機能

| タスク  | 名称                           | サイズ | 状態 | ブランチ/PR | 備考 |
| ------- | ------------------------------ | ------ | ---- | ----------- | ---- |
| T-301-1 | Part III 教材 JA: モジュール10 | M      | ⏳   | —           |      |
| T-301-2 | Part III 教材 JA: モジュール11 | M      | ⏳   | —           |      |
| T-301-3 | Part III 教材 JA: モジュール12 | M      | ⏳   | —           |      |
| T-301-4 | Part III 教材 EN: モジュール10 | M      | ⏳   | —           |      |
| T-301-5 | Part III 教材 EN: モジュール11 | M      | ⏳   | —           |      |
| T-301-6 | Part III 教材 EN: モジュール12 | M      | ⏳   | —           |      |
| T-302   | キャップストーン(分岐シナリオ) | L      | ⏳   | —           |      |
| T-303   | バッジ                         | S      | ⏳   | —           |      |
| T-304   | 修了証+サーバ側再検証          | L      | ⏳   | —           |      |
| T-305   | 用語集 (S-08)                  | S      | ⏳   | —           |      |
| T-306   | 検索 (S-09)                    | M      | ⏳   | —           |      |
| T-307   | ノート機能                     | M      | ⏳   | —           |      |

## Phase 4: 磨き込み

| タスク | 名称                 | サイズ | 状態 | ブランチ/PR | 備考 |
| ------ | -------------------- | ------ | ---- | ----------- | ---- |
| T-401  | a11y監査対応         | M      | ⏳   | —           |      |
| T-402  | 性能最適化           | M      | ⏳   | —           |      |
| T-403  | E2E拡充              | M      | ⏳   | —           |      |
| T-404  | EN校閲・整合最終確認 | M      | ⏳   | —           |      |

## スキル抽出タスク(docs/design/08 §3.8)

| タスク | 名称                                           | サイズ | 状態 | 実施タイミング                        |
| ------ | ---------------------------------------------- | ------ | ---- | ------------------------------------- |
| SK-01  | content-authoring スキル抽出                   | S      | ⏳   | T-110-1 マージ直後                    |
| SK-02  | exercise-authoring(SK-01のreferenceとして開始) | S      | ⏳   | T-110-1 マージ直後                    |
| SK-03  | api-route スキル抽出                           | S      | ⏳   | T-104 マージ直後                      |
| SK-04  | viz-component スキル抽出                       | S      | ⏳   | T-204 マージ直後・Wave 8 投入前に必須 |
| SK-05  | translate-module スキル抽出                    | S      | ⏳   | T-111-1 マージ直後                    |

---

## 状態の凡例

| 記号 | 意味                   |
| ---- | ---------------------- |
| ⏳   | 未着手                 |
| 🔵   | 進行中                 |
| 🔄   | レビュー中             |
| ⛔   | ブロック中(依存待ち等) |
| ✅   | 完了・マージ済み       |

---

## 決定事項ログ(設計からの逸脱・追加判断)

- 2026-07-17: ADR-007採用(Cloudflare Workers)。T-000検証項目にwrangler preview上の動作確認を追加。T-002にサーババンドルサイズゲートを追加。T-304の再検証実行環境をGitHub Actionsに変更。
- 2026-07-18: T-000実施中、`next-mdx-remote` + `node:fs/promises` によるリクエスト時MDX読込がwrangler preview環境で `[unenv] fs.readFile is not implemented yet!` により失敗することを確認(Workersにはリクエスト時に読めるファイルシステムが存在しないため)。`@next/mdx` によるビルド時コンパイルに切替えて解消。T-006(コンテンツビルドパイプライン)は `lib/content.ts` を実行時fs読込に依存させず、ビルド時に静的解決する設計とすること(詳細: docs/skeleton-notes.md)。
- 2026-07-18: T-001で02§5.2の「no-literal-jsx-text」規約を `eslint-plugin-react` の `react/jsx-no-literals` ルールで実装(同名の独立ルールは存在しないため)。このルールはJSX子要素直下の文字列リテラルを全て(句読点含む)禁止するため、`{t.x}: {y}` のように翻訳済み値の間に裸の記号(`:` `(` `)` など)を置く書き方はエラーになる。**恒久対策**: 今後同様のケースは `{`${t.x}: ${y}`}` のように単一のテンプレートリテラル式コンテナへ畳み込むこと(components/Lab.tsxで実施済み)。
- 2026-07-18: T-001の「ディレクトリ骨格(02§1どおり)」は、T-001のOut of Scope(機能実装/i18n/DB)およびCLAUDE.md規則3(モック・スタブ禁止)と衝突するため、app/[locale]再構成・app/api・prisma/schema.prisma・lib/db.ts・lib/content.ts・components/{viz,lab,mdx,ui}等の空スタブは作成しなかった。実体を伴わないディレクトリ作成は「実装したことにする」偽装と区別がつかないため、各ディレクトリは対応タスク(T-003/T-004/T-006/T-007/T-101+/T-203+)が実装と同時に作成する方針とする。T-001では既存の app/, components/, content/, lib/, messages/, types/ に加え、Vitestの実体テストを伴う tests/unit/ のみを新設した。
- 2026-07-18: T-001はツーリング整備タスクでUI新規実装を伴わないため当初qa-evaluatorを未実施だったが、components/Lab.tsx(既存UI)にlint適合のための機械的変更を加えていたため追加でqa-evaluatorを実施。ブラウザ操作(合格/タイムアウト/エラー、ja/en)で旧コミット(93fe2d5)と表示結果が完全一致することを確認しPASS。**恒久対策**: 既存UIコンポーネントに1行でも変更を加えた場合は、そのタスクが「UI系タスク」でなくてもqa-evaluatorでの回帰確認を完了報告前に行うこと。副次的発見として `lib/runner/harness.worker.ts` のエラーメッセージがロケール非依存で日本語ハードコードのまま(T-000由来の既存不具合)であることを検出。修正は本タスクのスコープ外のため実施せず、後続タスク(T-107b/T-107c想定)でのフォローアップ候補として記録。
- 2026-07-18: T-002実施中、GitHub Actionsのワークフローが未マージのためローカルでの実行結果確認ができず、`act`もCI環境に未導入だったため、`actionlint`(brewで導入)によるワークフローYAMLのスキーマ検証+全コマンド(lint/typecheck/test/build)のローカルexit 0確認で受入基準を代替した(WBS T-002本文に明記された代替手段)。**恒久対策**: `actionlint`は意図的な`if: false`プレースホルダーjobを`constant expression in condition`として警告するが、これはWBS T-002が明示的に要求する設計(content-validate/e2eをT-006/T-403待ちのplaceholderとして先に定義)であり偽陽性として無視してよい。また、`wrangler deploy --dry-run`が生成する`.worker-dryrun/`配下のバンドル済みJSは`.gitignore`だけでは`npm run lint`の対象から除外されず(ESLintは.gitignoreを見ない)lintが壊れたため、`eslint.config.mjs`のignoresにも同ディレクトリを追加。**今後、ビルド/検証用に一時生成する新規ディレクトリを追加する際は、`.gitignore`と`eslint.config.mjs`の両方に追記すること。**
- 2026-07-18: T-003実施。next-intl(^4.13.2、02§5.1/5.2が明示的に要求する依存)を導入し、`app/{ja,en}/demo`(T-000の静的ルート、docs/skeleton-notes.mdに「T-003で本来のmiddlewareベースのロケール解決に置換」と明記済み)を`app/[locale]/demo`+`middleware.ts`(next-intlのcreateMiddleware、URL>Cookie(NEXT_LOCALE)>Accept-Language>既定'en'を標準機能で満たす)へ置換。**失敗→恒久対策(1)**: `next-intl`のmiddlewareバンドルが拡張子なしで`next/server`をimportするが、`next`パッケージにexportsフィールドが無いためVite(vitest)のESM解決が失敗し(`Cannot find module .../next/server`)、さらにNode ESM外部化によりVite側の`resolve.alias`も素通りされる二重の問題があった。`vitest.config.ts`に`resolve.alias["next/server"]`(実ファイルへの明示的エイリアス)と`test.server.deps.inline: ["next-intl"]`(alias適用のため強制インライン化)の両方を追加して解消(片方だけでは解決しない)。**恒久対策**: 今後next-intl以外のnode_modules配下パッケージをvitestで直接importして単体テストする場合も、同様の「拡張子なし内部import + package.jsonにexports欠如」の組み合わせで同じ症状が起きうるため、同じ2点セットのconfig変更で対応すること。**失敗→恒久対策(2)**: `next-intl`のnavigation hooks(`usePathname`/`useRouter`、`useLocale`経由)はReact Server Component側で`NextIntlClientProvider`によるlocale contextを要求するが、`messages`propを省略すると`i18n/request.ts`の`getRequestConfig`から`messages`を解決しようとして「No messages found」で実行時エラーになる(ビルド自体は成功するため`npm run build`だけでは検出できず、`next start`でのアクセス確認で判明した)。本プロジェクトはUI文言を`lib/i18n/messages.ts`の自前カタログで管理しnext-intl本体のメッセージ機能を使わない方針のため、`NextIntlClientProvider`に`messages={{}}`を明示して回避。**恒久対策**: next-intlのProvider/hooksを部分的に(routing/navigationのみ)導入する際は、`npm run build`のグリーンだけでなく`next start`(または`next dev`)での実アクセス確認を完了報告前に必ず行うこと(SSG出力のプリレンダーがエラーを隠蔽しないことをbuild成功だけでは保証できない)。**失敗→恒久対策(3)**: T-001決定事項ログの「既存UIコンポーネントに1行でも変更を加えた場合はqa-evaluator必須」ルールを当初の完了報告で見落とし(components/DemoPage.tsxを変更していたにもかかわらずtest-integrity-reviewerのみで報告)、ユーザー指摘で追加実施したqa-evaluatorがFAIL(操作性3/5)を検出: `components/LocaleToggle.tsx`が`router.replace(pathname, {locale})`を使っていたためブラウザ履歴に新規エントリが積まれず、トグル後に戻るボタンを押すと`about:blank`へ抜けるリグレッション(T-000の`<Link>`ベース実装には無かった問題)。`router.push(...)`に変更して修正し、実ブラウザでの戻るボタン往復を再検証してPASSを確認した。**恒久対策**: 完了報告を作成する前に、CLAUDE.md/STATUS.mdの既存の「必須チェック」項目(qa-evaluator要否を含む)を機械的に再確認するチェックリストとして扱うこと。「このタスクはUI系ではない」という自己判断だけでqa-evaluatorを省略しない。
- 2026-07-18: T-010実施。`lib/contracts/`に03文書T-010行の成果物①〜⑤(API I/O zod、演習YAML zod、Worker⇄Mainメッセージ、SimEngine、slugマニフェスト)を実装し、正常系/異常系パーステストを各スキーマ群に整備(36 test)。依存追加: `zod`(^4.4.3、当タスクの受入基準が明示的に要求)。**設計差異(要判断)**: 03文書T-101/T-106行は「module.yamlスキーマ(T-010の型)」「quiz.yaml」を前提とするが、T-010行の公式成果物リスト①〜⑤にも02文書にもmodule.yaml/quiz.yamlのフィールド定義が存在しない。今回はスコープを03文書T-010行の①〜⑤に厳密限定し、これらのスキーマは追加しなかった(CLAUDE.md規則1: Out of Scope作業の禁止)。**恒久対策**: T-101/T-106着手前に、①追加の型確定タスクとして対応するか、②02文書へmodule.yaml/quiz.yaml構造を追記のうえ本タスクを再オープンするかの判断が必要(未解決のまま着手すると型がその場しのぎになるリスクがある)。
- 2026-07-18: T-004実施。インストールされた`prisma@7.8.0`はNode `^20.19 || ^22.12 || >=24.0`のみサポートしており、ローカルのデフォルトNode(v23.11.0、非LTS)では`prisma`コマンドがnpm engine警告のみでサイレントにexit 1し原因特定が困難だった(CIの`node-version: "22"`は該当範囲内のため無関係)。nodenvで22.15.0を導入して解消。**恒久対策**: 今後ローカルでPrisma CLIを直接実行する作業(T-005/T-104/T-109等)では、まず`node --version`を確認し、範囲外なら`nodenv install`等でCIと同系統(22.x)に揃えてから実行すること。
- 2026-07-18: T-004実施。Prisma 7では`schema.prisma`の`datasource`ブロックに`url`/`directUrl`を書くと`P1012 (property no longer supported)`で検証エラーになる(旧versionからの破壊的変更)。接続情報は`prisma.config.ts`の`datasource.url`(migrate/introspection用、`DIRECT_URL`優先)と、`PrismaClient`コンストラクタに渡す`adapter`(`@prisma/adapter-pg`、`DATABASE_URL`、lib/db.ts)の二箇所に分離する新方式に対応した。生成されたクライアント(`lib/generated/prisma/`)は`.gitignore`対象とし、`package.json`に`postinstall: prisma generate`を追加して`npm ci`後に必ず再生成されるようにした。**恒久対策**: 今後`lib/db.ts`を利用する全タスク(T-005/T-104/T-109等)は、DB接続を追加・変更する際にschema.prismaへ`url`を書き戻さないこと(P1012で即座に検証エラーになるため事故りにくいが、代わりに`prisma.config.ts`を編集する)。
- 2026-07-18: T-007実施。zustand+beforeInteractiveスクリプトで`<html>`にテーマクラスを注入する構成は、`suppressHydrationWarning`を付けないとダーク選択後の再読み込みで毎回Reactのハイドレーション不整合警告が出る(qa-evaluatorの実機検証で検出、`npm run build`単体・vitestでは検出不可)。加えて、未実装ページ(後続タスク待ち)へのHeader/Footer/AccountMenuのLinkは、本番ビルド(`next build && next start`)ではビューポート内自動prefetchにより**クリック等の操作なしでページを開いただけ**で404 consoleエラーが複数発生する(devモードでは再現せず`npm run dev`での確認だけでは見落とす)。**恒久対策**: (1)手動でDOM属性を注入してダーク/テーマ状態を表現するレイアウトでは、対象要素に`suppressHydrationWarning`を付与すること。(2)未実装ページへリンクするコンポーネント(後続タスク待ちのnot-foundフォールバック含む)は`prefetch={false}`を付与し、検証は`npm run dev`だけでなく`npm run build && npm run start`で「操作せずページを開いただけ」の状態のconsoleも確認すること(T-101/T-305/T-306/T-005/T-010でこれらのリンク先ページが実装された時点で、当該Linkの`prefetch={false}`除去を検討する)。
- 2026-07-18: T-007実施中に判明: このworktreeはT-004(DB/Prisma)がmainへマージされる前に分岐したため、T-004マージ後の未コミット状態で`git diff main`(二点)を取るとT-004成果物が「削除」であるかのように見える見かけ上のノイズが生じた(test-integrity-reviewerが検出)。実際にはT-007がT-004ファイルを一切変更していないことを`git diff main...HEAD`(三点、マージベース基準)で確認済み。コミット後に`git merge origin/main`でT-004を取り込み(package.json/package-lock.json/vitest.config.tsで3件の軽微なコンフリクト、いずれも追記の競合でロジック的な衝突ではなく解消済み)、マージ後に`npm run lint/typecheck/test/build/test:e2e`を再実行して全green確認した。**恒久対策**: 並列worktreeで長時間作業する場合、コミット前に`git fetch origin && git log --oneline main..origin/main`で他タスクの新規マージ有無を確認し、自分のT-xxx分の変更を先にコミットしてから`git merge origin/main`で追従する(作業ツリーが汚れた状態で直接mergeすると無関係ファイルとのコンフリクトで失敗するため、コミット→マージの順序を守ること)。
- 2026-07-18: T-006実施。`lib/content.ts`(node:fs/yamlによるビルド時MDX/YAMLロード+frontmatter検証。Cloudflare Workersのリクエスト処理経路からは直接importしない設計。T-000由来の制約を継承)、`scripts/validate-content.ts`(①ja/en slug集合一致②演習testsハッシュ一致③リンク切れ④frontmatter必須項目を1回の実行で全件収集・報告、成功時のみslugマニフェストJSON生成)を実装。検証用フィクスチャ5種(正常系+missing-slug/tests-mismatch/broken-link/missing-frontmatter)をtests/fixtures/content/に整備し、各異常系がファイルパス+原因を含む特定可能なエラーを出すことをvitestで担保(10 test)。依存追加: `yaml`(YAML構文解析)、`tsx`(devDependency、TypeScript CLIスクリプトの実行に必要。CIがNode 22でnode組み込みのTS型剥離が既定で効かないため)。CIの`content-validate`ジョブの`if: false`を解除(既存のjob DAG構造・`needs: build`は変更せず、T-002が定義した位置のまま有効化)。**設計判断**: module.yamlの構造(T-010決定事項ログで公式contract未定義と判明済み)は、T-006自身の検証(slug列挙・frontmatter必須項目)に必要な最小スキーマ(slug/title/order/minutes)としてlib/content.ts内にローカル定義し、lib/contracts/へは追加しなかった(規則2 + T-010の先行判断を継承)。quiz.yamlも同様の理由で構造スキーマは定義せず、YAML構文の妥当性のみ検証。リンク切れ検査はcontent/配下への相対参照のみを対象とし、外部URL・`#`アンカー・`/`始まりの絶対パス(将来のアプリルート)は対象外とした(アプリのルーティング構造はT-101以降で確定するため)。**恒久対策**: TypeScript製CLIスクリプトを追加する際は、CIのNode指定バージョン(現状22)でNode組み込みの型剥離が既定で効かない場合があるため、`tsx`等の実行手段を明示的に依存として追加すること(バージョン起因の暗黙的動作差に頼らない)。
- 2026-07-18: T-005実施。Auth.js v5(`next-auth@5.0.0-beta.31`)を導入。Credentialsのパスワードハッシュは`hash-wasm`のArgon2id(純WASM実装、ADR-007 C-3が要求する「workerdでも動くWASM実装」)を既定とし、WASM初期化・実行が失敗した場合のみ`node:crypto`のscrypt(Cloudflare Workersは`nodejs_compat`フラグ導入済みのため利用可能)へフォールバック(`lib/auth/password.ts`)。02§2.1の8テーブル(T-004で確定済み)にAuth.js標準のSession/Account/VerificationTokenテーブルは存在しないため、セッションはDB非依存のJWT戦略とし、OAuthアカウント連携は既存の`oauth_accounts`(user_id/provider/provider_account_id)のみを使うカスタムAdapter(`lib/auth/adapter.ts`)を実装した(`@auth/prisma-adapter`は使用せず)。**設計判断(パスワードリセット)**: 03文書T-005行はリセット画面の存在のみを要求し、リセットトークン用テーブルは02§2.1に定義がない。本タスクでの新規テーブル追加は規則1(Out of Scope作業禁止)に抵触するため、DBに保存しないステートレスJWT(`lib/auth/resetToken.ts`)を採用した。「使い切り」制約は、発行時点のpasswordHashダイジェストをトークンに埋め込み検証時に現在値と照合する方式で実現(リセット成功でハッシュが変わるため再利用不可になる)。また本プロジェクトにはメール送信基盤が未導入(07人間作業チェックリストにもSMTP系秘密情報の記載なし)のため、規則3(実装したことにする禁止)に従い「メールを送信した」という偽の成功文言は出さず、生成したリセットリンクを画面上に直接表示する設計とした。**設計差異**: 03文書のOAuth `callbackUrl`/Credentials成功後の遷移先について、T-007(共通レイアウト)・T-101(カリキュラム一覧)・T-112(ダッシュボード)が未実装のため遷移すべきホーム画面が存在しない。Credentials成功時は画面上に成功メッセージを表示するのみとし、OAuthの`callbackUrl`は暫定的に既存の`/demo`(Walking Skeleton)を指す。後続タスクでホーム/ダッシュボードが実装された時点で本来の遷移先に差し替えること。**失敗→恒久対策(1)**: `next dev`で`/ja/auth/signin`等を連続操作すると、ファイル未編集時でも`SyntaxError: Unexpected end of JSON input`によるRSCフェッチの500が散発することを確認した(原因: dev serverのオンデマンドコンパイル・prefetchとRSCストリーミングの競合と推定、next-authに限らない一般的なNext.js 15 dev modeの既知の揺らぎ)。`npm run build`→`next start`に切り替えたところ同一操作で0エラーとなり再現しなくなった。**恒久対策**: 認証など複数リクエストが連鎖する画面のブラウザ検証は、`next dev`ではなく`npm run build && next start`で行うこと(T-003決定事項ログの「next startでの実アクセス確認」をより厳格化)。**失敗→恒久対策(2)**: `tests/unit/auth/oauthButtons.test.tsx`でコンポーネント関数を直接呼び出すテストを本プロジェクトで初めて追加した際、`vitest.config.ts`にJSX変換方式の指定がなくesbuildの既定(classic変換)で「React is not defined」となった(Next.js本体はSWCでautomatic runtimeを使うため今までのdev/build検証では顕在化しなかった)。`vitest.config.ts`に`esbuild.jsx: "automatic"`を追加して解消。**恒久対策**: 今後.tsxコンポーネントをレンダラを介さず直接単体テストする場合、このconfigが既に効くため追加対応不要。次回別の理由で類似エラーが出た場合はこの設定の有無をまず疑うこと。依存追加: `hash-wasm`(Argon2id/フォールバックscryptの由来)、`jose`(パスワードリセットJWTの署名/検証)、`next-auth`(Auth.js本体)。**失敗→恒久対策(3)**: 初回qa-evaluatorがFAIL(操作性3/5)を検出: `middleware.ts`のレート制限が`/api/auth/*`配下の全メソッド(GETの`/api/auth/csrf`・`/api/auth/providers`含む)を同一の5req/min予算でカウントしていたため、`next-auth/react`の`signIn()`が1回の試行で発行する複数リクエスト(GET csrf→POST callback)により、パスワードを打ち間違えて2回目を試すだけの正常なユーザーが制限に到達し、「サインイン中…」表示のままハング+未捕捉例外(`Failed to construct 'URL': Invalid URL`)、または`/api/auth/error`への強制遷移という重大な操作性劣化を引き起こしていた。`middleware.ts`をPOSTのみカウント(資格情報を実際に検証・変更するアクションのみ)に変更し、`SignInForm`/`SignUpForm`の`signIn()`呼び出しをtry/catchで保護、`ResetRequestForm`/`ResetConfirmForm`は429を明示判定し共有i18nキー`auth.rateLimited`(ja/en対で追加)を表示するよう修正。再検証qa-evaluatorでPASS(操作性5/5)を確認。**恒久対策**: 今後IPベースのレート制限をミドルウェアに追加する場合、対象を「実際に副作用を持つ操作(状態変更・資格情報検証)」に限定し、同一フローが内部的に発行する前段の読み取り専用リクエスト(CSRFトークン取得等)まで同じ予算に含めないこと。クライアントSDK(next-auth/react等)が1操作あたり何リクエスト発行するかを実ブラウザで確認せずに予算設計をしないこと。**失敗→恒久対策(4)**: PR作成後、CIの`build`ジョブが`Error: DATABASE_URL is not set`で失敗(ユーザー指摘で発覚)。原因は`lib/db.ts`(T-004)の`export const prisma = ... ?? createPrismaClient()`がモジュール読み込み時点でPrismaClientを即時生成する実装だったため。T-005で追加した`app/api/auth/**/route.ts`が`lib/auth/config.ts`経由で`lib/db.ts`をimportするようになった結果、`next build`の「Collecting page data」(クエリを実行せずRoute Handlerモジュールをimportして静的解析する工程)がこのimportを評価するだけでエラーになった。ローカルでは`npm run build`を実行するたび毎回シェルにDATABASE_URLをexport済みだった(DB検証作業を同一セッションで並行していたため)うえ、Next.jsがリポジトリ直下の`.env`(DATABASE_URL設定済み)を自動読み込みするため、シェル側で`unset`しても`.env`の値で上書きされず「DATABASE_URL未設定のビルド」を一度も再現できていなかった(CIの`build`ジョブは`.env`を持たずDB接続情報を一切渡さないため、ローカルとCIで実際には異なる環境をテストしていたことに気づけなかった)。`lib/db.ts`をProxy経由の遅延初期化に変更し(初回プロパティアクセス時のみ`createPrismaClient()`を呼ぶ)、モジュールimport自体はDATABASE_URL不要にして解消。`DATABASE_URL="" npm run build`で再現・修正確認、`npm run test:integration`(実DB、$connect/$disconnect含む)で回帰なしを確認。**恒久対策**: ①`lib/db.ts`のような「importされただけで外部リソースの初期化を要求する」モジュールは、Route Handler/Pageからimportされうる時点でビルド時静的解析の対象になりうるため、必ず遅延初期化にすること(モジュール評価と実際の副作用実行を分離する)。②ローカルで「envなしビルド」を検証する際は、シェルで`unset`するだけでなく`DATABASE_URL=""`のように明示的に空文字で上書きするか`.env`を一時退避しないと、Next.jsの`.env`自動読み込みにより意図した検証にならない。CIジョブが渡さない環境変数がある場合、ローカルでも同じ変数を明示的に空にして`npm run build`を実行してからpushすること。
- 2026-07-19: T-010決定事項ログ(2026-07-18付)で保留していた「module.yamlスキーマの公式contract化」を実施。T-006がT-006検証専用として`lib/content.ts`内にローカル定義していた`ModuleMetaSchema`(slug/title/order/minutes、フィールド変更なし)を`lib/contracts/module.ts`へそのまま昇格し、`lib/content.ts`は昇格後のcontractをimportして使う形に変更した(既存のT-006テスト・フィクスチャは無変更で全green)。**設計判断**: S-02の「3部(Part I〜III)のセクション分割」は、02文書がpartフィールドを定義せずfrontmatter必須項目もtitle/order/minutesのみのため、module.yamlに`part`フィールドは追加しない。Part境界(モジュール1-4=Part I, 5-9=Part II, 10-12=Part III)はT-101の実装内でorderの範囲から導出すること。quiz.yaml構造は今回のスコープに含めず、T-106着手前に別途同様の判断が必要な状態のまま(引き続き構文検証のみ)。これによりT-101(カリキュラム一覧)が依存未充足(CLAUDE.md規則10)で停止する状態を解消した。
- 2026-07-19: T-101実施。`app/[locale]/learn/page.tsx`(S-02)を実装。module.yamlスキーマ(`lib/contracts/module.ts`)をベースにPart境界(T-010決定事項ログの判断: order 1-4/5-9/10-12)で3部へ分割し、`components/curriculum/{CurriculumList,ModuleCard,ProgressRing,OrderConnector}`で描画(ロック無し+`OrderConnector`で推奨順を矢印表示、進捗リングはprops注入のみ)。**失敗の未然防止(恒久対策の先行適用)**: 実装前の設計検討で、`lib/content.ts`(node:fs依存、T-000/T-006決定事項ログにより「Cloudflare Workersのリクエスト処理経路から直接importしない」制約が既知)を`app/[locale]/learn/page.tsx`から直接呼び出すと、OpenNextのCloudflareアダプタがキャッシュバックエンド(R2)未設定のためプリレンダー済み出力を使えずページ関数をリクエスト時に再実行する(T-000決定事項ログの一般的挙動)結果、fs呼び出しがバンドルに含まれたままWorker実行時に評価され、T-000で確認済みの`[unenv] fs.readFile is not implemented yet!`を再現するリスクを特定した。対応として`scripts/generate-curriculum.ts`(Node CLI、`lib/content.ts`を呼び出しビルド時のみ実行)が`lib/generated/curriculum.{ja,en}.json`(`.gitignore`済み、T-004のPrisma生成物と同様の生成物ディレクトリ規約)を書き出し、`lib/curriculum.ts`はこれを通常のESM importとして取り込む(`@next/mdx`がMDXに対して行う「ビルド時解決」と同型の対策)構成とし、`npm run{build,typecheck,test,dev}`にpre-hookとして`generate:curriculum`を追加した。`npm run build`のグリーンだけでは実行時再実行の問題を検出できないため(T-003/T-007決定事項ログの教訓を継承)、`npx opennextjs-cloudflare build && npx wrangler dev`で`/ja/learn`・`/en/learn`が200(fsエラー無し)であることを実機確認した。**恒久対策**: 今後`lib/content.ts`(またはnode:fs依存の他のビルド時専用モジュール)をpage.tsx/route.tsから利用する場合は、直接importせず「Node CLIスクリプトで静的JSON化→通常importで取り込む」構成を既定パターンとすること。`npm run build`成功のみで良しとせず、fs依存を新たに経路に組み込んだ変更は`wrangler dev`(または`opennextjs-cloudflare build`+preview)での実アクセス確認を完了報告前に行うこと。**設計判断**: 02§4.3「ロック概念は設けない。ただし推奨順を矢印で示す」は、進捗データ(T-105実装対象)と無関係な構造的アフォーダンスと解釈し、`OrderConnector`をPart内の各モジュール間に常時表示する(進捗の完了/未完了状態に依存させない)方式とした。`components/layout/Header.tsx`の`/learn`リンクは、T-007決定事項ログが明記した「実装完了時に除去を検討する」対象だったため`prefetch={false}`を除去した(T-101自身の成果物に直結する1行修正のためスコープ内と判断)。フィクスチャ12モジュール(`tests/fixtures/curriculum/{ja,en}/`)による順序描画のスナップショットテスト(`tests/unit/curriculum/CurriculumList.test.tsx`、ja/en各1件)と`groupModulesByPart`/フィクスチャロードの単体テスト(`tests/unit/curriculum.test.ts`、6件)を追加。content/配下には実カリキュラム教材が存在しない(T-110/T-111が未着手)ため、本番ビルドの`/learn`は現時点でモジュール0件のPart見出し無し状態で描画される(想定どおり、コンテンツ追加はスコープ外)。
- 2026-07-19: T-107a実施。`lib/runner/harness.worker.ts`をT-000 Walking Skeleton原型から本実装へ更新(型はT-010で確定済みの`lib/contracts/runner.ts`に統一、`lib/runner/types.ts`はT-107c(jsRunner.ts/exerciseFixture.ts)がまだ依存しているため無変更のまま残置)。02§7.1手順1〜3,5を実装: ①禁止トークン検出(`importScripts`/`fetch`/`XMLHttpRequest`/`Atomics.wait`/`eval`/`new Function`を単語・呼び出し境界の正規表現で検出。単純な部分一致ではなく`\bfetch\s*\(`のような形にし、`prefetchData`等の無関係な識別子を誤検知しない。※テンプレ許可制はこの精度向上として解釈——用語がdocs内で他に説明されていないため設計差異として明記)②危険グローバル無効化(`fetch`/`XMLHttpRequest`/`WebSocket`/`importScripts`を`globalThis`上でundefined化、実行後に復元)③Blob URL経由のESM dynamic import④console捕捉(200件上限)⑤結果1MB上限+`truncated`フラグ(pass/fail結果は`perTest`の`actual`/`error`を段階的にクランプ、error結果は`error`文字列自体をクランプ)。**設計判断**: `runHarness(request, deps)`はモジュールローダ(既定=Blob URL経由の実装)を`deps.loadModule`で差し替え可能にした。Node/Vitestの標準ESMローダは`blob:`スキームの動的importを解決できない(`Only URLs with a scheme in: file, data, and node are supported`)ため、jsdom追加(規則1の無許可依存追加に抵触)を避けつつ受入基準の4系統(禁止トークン検出/console上限/結果サイズ上限/import失敗時のerror返送)を決定的にテストする手段としてDIを採用(worker本体の`ctx.onmessage`配線は実運用のBlob URL実装のみを使う)。test-integrity-reviewerでこの設計が「要件を隠すモック」ではないことを確認済み。**失敗→恒久対策**: 初回実装で`truncateResult`の"error"分岐が`logs`のみクランプし`error`文字列自体を上限対象にしていなかった(巨大な例外メッセージで1MB超過しうる欠陥、test-integrity-reviewerが検出)。`error`にも`clampString`を適用して修正し、巨大`error`メッセージ(200万文字)を注入する回帰テストを追加(修正前は実際に失敗することを確認済み)。**恒久対策**: 構造化結果に複数の可変長文字列フィールド(logs/perTest別/error等)が混在する場合、サイズ上限のクランプ処理は「先頭の一部だけ」ではなく型で分岐する全フィールドを網羅すること、実装時にどのフィールドが未クランプかをチェックリスト化すること。**環境上の発見(タスク固有ではない)**: このworktreeは`node_modules`が空の状態で作成され、`npm run typecheck`/`build`がnext-intl/next-auth/zustand/hash-wasm/jose/@radix-ui/@playwright/prisma生成クライアント等「Cannot find module」で失敗した。mainブランチの実チェックアウト側でも同一の失敗が再現し(`npm run typecheck`の出力がworktree側と完全一致、diff結果ゼロ)、T-107a固有の問題ではなく環境未セットアップに起因すると判断した。さらに素の`npm install`はPrisma 7.8.0のpreinstallスクリプトがNode v23.11.0(このマシンの実行時バージョン)を`^20.19 || ^22.12 || >=24.0`の対象外として拒否し失敗する。`npm install --ignore-scripts`→`npx prisma generate`の順で回避し、以降lint/typecheck/test/buildは全green(T-107a差分に起因する新規エラーはゼロ)。**恒久対策**: 新規worktreeでこのプロジェクトの検証コマンドを実行する前に、`node_modules`が実際に存在し`npm run typecheck`が(自分の変更を除いた状態で)green出発点であることを最初に確認すること。素の`npm install`がPrisma engine-gateで失敗する場合は`npm install --ignore-scripts && npx prisma generate`で回避可能。この問題はワークツリー間でnode_modulesが共有されない/Node v23実行環境という一時的な設定に起因し、恒久的なCI設定の問題ではないため`.claude/`やCI定義の変更は行わなかった。
- 2026-07-19: T-107b実施。`lib/runner/grader.ts`(新規)を実装。02§7.2の4種assert(`equals`/`deepEquals`は設計表で同一挙動「構造比較・NaN/-0対応」と定義されているため同一の比較関数`structuralEquals`を共有、`oneOf`/`matches`/`property`)、部分点計算(`gradeExercise`: `score=round(passed/total*100)`、全合格時のみ`result:"pass"`)、失敗時diff生成(`diffValues`、オブジェクト/配列は再帰的にキー・要素単位で差分行を生成、深さ5で打ち切りJSONフラット表示にフォールバック)、`graderVersion`定数(初版"1.0.0")、演習別ヘルパ登録機構(`evaluateProperty`+`PropertyHelperRegistry`、`check`文字列をeval/Function不使用の自前再帰下降パーサ`parseCheckExpression`で解析)、complexity参考表示(`estimateComplexity`、合否に不使用)を実装。**設計判断**: このモジュールは値の比較・採点ロジックのみを担い、ユーザーコードのロード・サンドボックス化(`harness.worker.ts`、T-107a)とは意図的に分離し、export解決を`resolveFn: (name) => unknown`としてDIすることでWorker/Node双方から利用可能にした(実際の配線・`RunRequest`/`RunResult`コントラクトへの統合はT-107c「Runner統合」の責務であり、依存タスク未着手のため`lib/contracts/`・`harness.worker.ts`・`jsRunner.ts`は一切変更していない)。**失敗→恒久対策(1)**: 初回実装で循環参照検出(`hasCircularReference`)と構造比較(`structuralEqualsInternal`)を再帰で実装したところ、3000階層規模の深いネスト(受入基準の必須エッジケース)で`RangeError: Maximum call stack size exceeded`が発生した(test-integrity-reviewerの指摘を待たず自己検証で検出)。両関数を明示スタック(ワークリスト)による反復実装に書き換え、50,000階層でも例外なく処理できることを回帰テストで確認した。**恒久対策**: 「深いネスト」を受入基準に含む純粋関数は、実装時に再帰ではなく明示スタックによる反復を既定とすること。テストは数千階層程度で妥協せず、素の再帰実装なら確実に破綻する規模(数万階層)で検証し、かつ深さ上限(diffの`MAX_DIFF_DEPTH`等)を導入する場合はその上限が実際に発火することを境界値(上限×2階層の入力)で確認すること(`toBeGreaterThan(0)`のような緩い検証は上限コードを削除しても通ってしまうため不可、test-integrity-reviewerが指摘し修正)。**失敗→恒久対策(2)**: `matches`アサートで非文字列(`NaN`等)を照合用に文字列化する際、当初`JSON.stringify`を用いたため`JSON.stringify(NaN)`が`"null"`になり`NaN`と`"^NaN$"`の照合が意図せず失敗するバグを自己検証(エッジケーステーブル駆動テスト)で検出した。プリミティブは`String()`(`String(NaN)==="NaN"`)、オブジェクト/配列のみJSON表現、に分岐する`stringifyForMatch`を導入し修正。**恒久対策**: 値を「表示用文字列化」する処理を複数種類(ログ用/diff用/正規表現照合用)持つモジュールでは、`JSON.stringify`はNaN/undefined/循環参照などJSONの丸めで意味が変わる値があることを前提に、用途ごとに変換関数を分けること(1つの`safeStringify`で全用途を賄おうとしない)。test-integrity-reviewerで自己検査を実施し、重大な整合性違反なし(循環参照/深いネストの回帰テストは実装を壊せば検出できる意味あるテストと確認、モックによる偽装なし)を確認済み。**失敗→恒久対策(3、CI検出)**: `package.json`の`prebuild`/`pretypecheck`/`pretest`キーがT-101(`generate:curriculum`)とT-006/T-104(`validate:content`)それぞれの作業で重複定義されており(JSON/JS object literalは同一キーの後勝ち)、クリーンチェックアウトでは`generate:curriculum`が実行されず`lib/curriculum.ts`が生成JSON不在で失敗する状態だった。ローカルでは既存の`node_modules`/生成物が残っていたため気づけず、GitHub Actionsの`typecheck`ジョブ(クリーンチェックアウト)で初めて顕在化した。`package.json`の3キーを`npm run generate:curriculum && npm run validate:content`の直列実行に統合して解消(グレーディング対象外の共有設定ファイルだが、CIブロッカーとしてユーザー指示により本タスクで修正)。合わせて`.gitignore`に`lib/generated/`・`content/generated/`を追加した(T-101/T-104決定事項ログが「`.gitignore`対象」と記載していたが実際には未記載だったための整合)。`lib/generated`(カリキュラムJSON+Prisma Client)・`content/generated`を退避した状態からの`npm run{lint,typecheck,test,build}`で回帰なしを確認。**恒久対策**: ①同一npm scriptキーを複数タスクが独立に追加する可能性がある場合、`package.json`のような共有設定ファイルはPRのdiffレビューで「同名キーの2回目の出現(後勝ちで1回目が無効化される)」を明示的に確認すること(JSONは重複キーをパーサ/エディタが警告しないため気づきにくい)。②「ローカルの検証green」は既存生成物やnode_modulesのキャッシュにより偽陽性になりうるため、共有フック(pre*/post*スクリプト)を変更するタスクは、ローカルでも対象の生成物ディレクトリを退避してから検証コマンドを実行し、クリーンチェックアウト相当の状態で確認すること。
- 2026-07-19: T-104実施。`PUT/GET /api/progress`(`app/api/progress/route.ts`)、ストリーク計算の純粋関数(`lib/progress/streak.ts`)、slugマニフェスト照合(`lib/progress/slugManifest.ts`、ビルド時生成の`content/generated/slug-manifest.json`を静的import)、ダブルサブミットcookie方式のCSRF検証(`lib/api/csrf.ts`)を実装。02§3.1の代表I/O(PUT/GETとも)、`itemSlug`未知→409、status後退(done→in_progress)の無視、ストリークの日跨ぎ加算/空白リセットに対応。**設計判断**: `newBadges`はPutProgressResponseSchemaが要求するフィールドのため常に空配列で返す(バッジ付与条件の評価はT-303の公式スコープ、STATUS.md T-303行「進捗API応答へのnewBadges統合」参照。規則1のOut of Scope作業に抵触するため評価ロジックは実装していない)。**失敗→恒久対策(1)**: `content/generated/slug-manifest.json`は`.gitignore`対象のビルド生成物で、`npm run build`単体は`validate:content`を自動実行しないため、クリーンチェックアウト(CIの各ジョブは`npm ci`のみ)では静的importが解決できずlint/typecheck/test/buildが失敗しうる状態だった。`package.json`に`prelint`/`pretypecheck`/`pretest`/`prebuild`/`pretest:integration`(npm既定のpre-hook、対応するスクリプト実行前に自動実行される)として`validate:content`(統合テストのみ`tests/fixtures/content/valid`を対象にした既知slugで生成)を追加し、各検証コマンド単体実行で常にマニフェストが最新化されるようにした。**恒久対策**: 今後`content/generated/**`や`lib/generated/**`のようなgitignore対象のビルド生成物を静的importで参照するモジュールを追加する場合、生成コマンドをconsumeする側の検証コマンド(lint/typecheck/test/build)のpre-hookとして必ず登録すること(postinstallだけでは`npm ci`を経ないローカル再実行時に古いまま取り残される)。**失敗→恒久対策(2)**: `tests/integration/progress.flow.integration.test.ts`追加後、`npm run test:integration`が`Foreign key constraint violated on the constraint: progress_user_id_fkey`で不安定に失敗した。原因はVitestの既定のファイル並列実行(`fileParallelism: true`)で、`tests/integration/db.crud.integration.test.ts`の`beforeEach`(全テーブル`deleteMany()`)が別ファイルで実行中のテストが作成した`User`行を消し飛ばしていたため(単一ファイルでは顕在化しない、テスト用DBを共有する2ファイル目の追加で初めて発覚)。`vitest.integration.config.ts`に`test.fileParallelism: false`を追加し直列実行に固定して解消。**恒久対策**: 今後`tests/integration/**`に全件`deleteMany()`するテストを追加する場合、既存ファイルとの共有DB競合を前提に、新規ファイル追加のたびに`npm run test:integration`をローカルで実行して安定性を確認すること(単体テストのpass/failだけでなく、並列実行由来のflakinessは複数ファイルが揃って初めて再現する)。
- 2026-07-19: T-102実施。`app/[locale]/learn/[module]/page.tsx`(S-03)を実装。`lib/moduleDetail.ts`(`buildModuleToc`: レッスンをfrontmatter order昇順→クイズ→演習の順に並べる、`nextItemHref`: 目次先頭アイテムへのパス、`tocItemHref`/`tocItemKey`)を新設し、`components/module/ModuleDetail.tsx`で目次・所要時間合計(module.yamlのminutes)・次アイテム導線CTAを描画。存在しないslugは`notFound()`で404。T-101の`scripts/generate-curriculum.ts`(ビルド時JSON生成パターン)に`generateModuleDetail`を追加し`lib/generated/module-detail.{ja,en}.json`を書き出す形でfs非依存を維持(T-101決定事項ログの恒久対策を継承)。演習YAML(`ExerciseDefinitionSchema`)にはtitleフィールドが無いため(規則2でlib/contracts変更不可)、演習の目次表示は「演習{index}」という通し番号ラベルとした(演習名はT-108以降のcontracts拡張が必要になった場合に別タスクで判断)。フィクスチャ(`tests/fixtures/moduleDetail/{ja,en}/`、レッスン2件+quiz.yaml+演習1件のモジュールと、空モジュールの2種)による目次組み立て/次アイテム導線の単体テスト(`tests/unit/moduleDetail.test.ts`)、コンポーネント描画のスナップショットテスト(`tests/unit/module/ModuleDetail.test.tsx`、ja/en×通常/空状態の4件)、存在しないslugでのnotFound()例外(digest `NEXT_HTTP_ERROR_FALLBACK;404`)を検証するページレベルテスト(`tests/unit/module/page404.test.ts`)を追加。test-integrity-reviewer実施し問題なしを確認。**失敗→恒久対策**: 着手時、クリーンな`npm run typecheck`が`package.json`の`prebuild`/`pretypecheck`/`pretest`キーがそれぞれ2回定義されている(JSONオブジェクトリテラルの重複キーは後勝ち)ことに起因して`generate:curriculum`のpre-hookが黙って消失し、`lib/curriculum.ts`の`Cannot find module './generated/curriculum.ja.json'`で失敗する状態になっていることを発見した。原因はT-101(`generate:curriculum`をpre-hookに追加)とT-104(同じキー名で`validate:content`をpre-hookに追加、STATUS.md該当行「失敗→恒久対策(1)」参照)が異なるPRで同名キーに別の値を追加し、git差分は行単位のためJSON意味論上の重複が機械的に検出されずマージされてしまったため(mainへのマージ後、誰も新規cleanチェックアウトで`npm run typecheck`を再実行していなかったため発覚が遅れた)。T-102はS-03単体では影響を受けないように見えたが、受入基準(1)の検証コマンド自体がこの理由で全滅する状態だったため、`prebuild`/`pretypecheck`/`pretest`の各キーを`npm run generate:curriculum && npm run validate:content`の単一エントリへ統合し解消した(package.jsonはT-102の直接成果物ではないが、検証コマンドを成立させるために必須の修正と判断)。**恒久対策**: 今後package.jsonの`scripts`に同名キーを追加するPRが複数同時に走る可能性がある場合、マージ後に必ず`npm ci`相当のクリーン環境(または少なくとも新規worktree)で対象コマンドを1回実行し、pre-hookが期待どおり両方の副作用(このプロジェクトでは`generate:curriculum`+`validate:content`)を発火させることを確認すること。JSON構文的にはvalidでもオブジェクトキー重複はTypeScript/ESLintの通常のCIチェックでは検出されない(esbuild/tsxはビルド時に警告を出すのみで失敗しない)ため、`git diff`のレビュー時にpackage.jsonの`scripts`セクションは特に注意して目視すること。
